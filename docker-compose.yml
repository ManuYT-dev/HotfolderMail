services:
  hotfoldermail:
    build: .
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs                 # logs visible on your host
      - /mnt/daten:/mnt/daten
    restart: unless-stopped